#include "../../binary_tree.h"
#include <vector>

std::vector<int> postorderMorris(TreeNode* root){
        std::vector<int> res;
        if(!root) return res;

        //to do
        //...

        return res;
}